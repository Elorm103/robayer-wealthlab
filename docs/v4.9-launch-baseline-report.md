# Version 4.9 — Launch Baseline Report (Final)

**This is the authoritative Launch Baseline, computed by the deployed code and verified directly against production D1.** It supersedes the two interim reports from the classification phase — those were reasoned manually before the classification-aware dashboard existed; this one reflects what the live system actually computes today, plus every metric Phase 9's baseline schema was built to capture.

No `production_launch_baselines` snapshot has been formally captured through the admin UI yet (that action is a deliberate, super_admin-triggered event — see `/admin/settings/`, "Production Launch Baseline"). The figures below are the same numbers that action would record if captured right now; capturing the official first entry is left as the founder's own action, not something this pass performs on their behalf, since Phase 9 explicitly treats a capture as an intentional, once-per-milestone business decision.

---

## Official baseline (Analytics Mode: Production Only)

| Metric | Value |
|---|---|
| Lifetime revenue (Production) | **GH₵0.00** |
| Total customers (Production) | **0** |
| Completed orders (Production) | **0** |
| Products (active, Production) | **1** — `starting-to-invest-with-gh100` |
| Bundles (active, Production) | **0** |
| Resources (published, Production) | **1** |
| Downloads (Production, real customer entitlement use) | **0** |
| Newsletter subscribers (Production) | **0** |
| Reviews (approved, Production) | **0** |
| Conversion rate | No qualifying activity in the trailing 30-day window |
| Average order value | N/A — no Production orders to average |
| Traffic (page views, lifetime, all sources) | **218** — not classification-scoped; `analytics_events` is anonymous, unauthenticated traffic data |

**One change since the classification phase's interim reports**: a second product, `momo-savings-playbook`, now exists in the catalog with status `coming-soon` (still `PRODUCTION`-classified, correctly — it is real, not test, content — but not yet purchasable, so it does not count toward the active-Production-products figure above). This is real platform activity that occurred between the classification pass and this final report, not a discrepancy in either report.

---

## Revenue breakdown (all-time, every classification — always shown regardless of Analytics Mode)

| Tier | Amount | Transactions |
|---|---|---|
| Production | GH₵0.00 | 0 |
| Internal | GH₵14.30 | 4 |
| Development | GH₵663.00 | 17 |
| **Total Processed** | **GH₵677.30** | **21** |

Identical to the prior interim report — confirms no data drift in the financial ledger since classification was applied, and confirms the deployed `getRevenueBreakdown()` computes the same figures a direct SQL query against production D1 produces.

## Customers by tier

| Tier | Count |
|---|---|
| Production | 0 |
| Internal | 4 |
| Development | 2 |
| Unknown | 0 |
| **Total** | **6** |

---

## Still open, per rule 7 (never guessed, never auto-archived)

- 18 anonymous abandoned `purchase_sessions` — `UNKNOWN`.
- 3 newsletter subscribers (`ajuik7449@gmail.com`, `robertloh727@gmail.com`, `thewatchmansv@gmail.com`) — `UNKNOWN`.
- 3 media assets — `UNKNOWN`.

All three groups are now resolvable through the new Archive Centre (`/admin/archive/`) — filter to `UNKNOWN`, open a record's evidence and related entities, and promote it with a reason if and when real evidence emerges. Nothing was auto-resolved by this milestone.

---

## Platform state at this baseline

- **Schema**: 32 migrations applied (`0001` through `0032`).
- **Audit trail**: 585 recorded events (grown from 569 at the start of this session's classification work — the increase reflects this session's own admin activity, testing, and verification work, not customer-facing events).
- **Deployed Worker version**: `efe28f82-6348-4786-964f-9a31dd36fcdd`.
- **Frontend commit**: `2b90e1b`.

## Bottom line

Version 5.0 begins from a verified GH₵0.00 Production baseline — the same number whether read from the dashboard, the Archive Centre, or a direct query against the database, because they all now compute it the same way. The platform's technical readiness has never been the open question; what changed this milestone is that every number describing "where things stand" is now honest, classification-aware, auditable, and — for the moment it's formally captured — immutable.
