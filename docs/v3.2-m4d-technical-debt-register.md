# Version 3.2 - Sprint M4D - Technical Debt Register

Consolidates every finding from this Board's independent review into one classified register: **Blocking** (must resolve before M4 can be accepted at all), **Closeout** (must resolve before M4E Closeout completes, but does not block this acceptance decision), or **Future Milestone** (real, tracked, but appropriately deferred).

---

## Blocking

**None.** No finding in this review rises to a level that prevents M4's overall scope, architecture, security posture, or functional correctness from being accepted.

## Closeout (Required Before M4E Completes)

| # | Item | Source | Severity |
|---|---|---|---|
| 1 | Add automated test coverage for the discount→receipt→PDF pipeline (`orderService.ts` with nonzero `discountPesewas`; `receiptPdfService.ts` discount-line rendering, both present and absent cases) | Testing Assessment, Functional Review | Moderate - financial-record-accuracy code with prior history of a real bug in this exact file (M2C MAR closeout) |
| 2 | Add a rollback-strategy section to `v3.2-m4c-database-changes.md`, matching M2's own established documentation convention | Documentation Review, Finding 2 | Low - the migration is purely additive and a rollback is straightforward to write, it just isn't written |
| 3 | Add the Customer Journey Validation's required disclosure ("Reviews launches with no automated collection mechanism until M5's trigger ships") to `v3.2-m4c-known-limitations.md` | Amendment Compliance Report (Amendment 3), Documentation Review Finding 3 | Low - the underlying UX behavior is already correct; this is purely a documentation-completeness item M4B explicitly required |
| 4 | Add a dedicated UX-design rationale note for Reviews' empty/sparse launch state, even briefly, so the design decision is traceable to M4B's requirement rather than only inferable from the shipped copy | Amendment Compliance Report (Amendment 3) | Low - same underlying gap as #3, different required artifact |
| 5 | Extend `checkFirstPurchaseOnlyViolation` test coverage to include the actual violation-detected case (a customer with a genuine prior purchase), not only the no-violation cases | Testing Assessment, gap #2 | Low - best-effort/detection-only by design, so a gap here is lower-stakes than #1, but still closeable cheaply |
| 6 | Reconcile the M4C documentation filenames against this review sprint's own "Mandatory Review" list (or explicitly ratify the `m4c`-infixed convention as correct going forward, so future review sprints reference the real paths) | Documentation Review, Finding 1 | Low - purely a cross-reference/process-hygiene item |

## Future Milestone (Correctly Deferred, Not M4C's Responsibility)

| # | Item | Source | Notes |
|---|---|---|---|
| 7 | Authenticated-admin-UI-level test coverage (real browser session against `admin-reviews.js`/`admin-coupons.js`, not just the API they call) | UX & Accessibility Review, Testing Assessment gap #3 | Requires either a local-dev admin-account bootstrap flow or a hashed-password seeding utility that does not currently exist anywhere in this codebase - a real, reusable piece of dev-tooling infrastructure, not scoped to M4C alone. Worth building once, benefiting every future milestone's own admin-UI verification, not re-solved ad hoc each time. |
| 8 | Admin Coupons' "Applies to" product dropdown has no search/pagination (fetches up to 100 products in one request) | `v3.2-m4c-known-limitations.md` (already correctly tracked there) | Not a defect against the catalog's real current size; correctly flagged as a future scaling concern already |
| 9 | No bulk admin actions (approve-all-pending reviews, bulk-deactivate coupons) | `v3.2-m4c-known-limitations.md` (already correctly tracked there) | Same - correctly deferred given today's realistic volumes |
| 10 | No dedicated "My Reviews" customer dashboard page | `v3.2-m4c-known-limitations.md` (already correctly tracked there) | The underlying API (`GET /api/customer/reviews`) exists and is tested; only the dashboard UI surface is missing, correctly named as a natural small follow-up |
| 11 | Admin review-table star-rating rendering has no numeric text equivalent for screen readers (minor, admin-only) | UX & Accessibility Review | Low priority, internal tool only, not a Level A failure the way the now-fixed public-facing issue was |

## Carried-Forward, Already-Accepted Risk (Not Debt)

For completeness, distinguished explicitly from genuine debt: the redemption-limit race's "loser still gets the discount" outcome and `first_purchase_only`'s detection-only enforcement are both **deliberate, reasoned architectural decisions**, independently re-verified sound by this Board (see Security Review, Functional Review), not deferred work or unresolved debt. They should not be tracked in a future-milestone backlog as if they were gaps to eventually close - they are the correct, permanent design.
