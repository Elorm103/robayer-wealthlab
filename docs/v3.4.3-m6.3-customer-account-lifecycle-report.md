# Version 3.4.3 M6.3: Customer Account Lifecycle Report

## Purpose

Traces the completed production purchase (`RWL-2026-000046`) through every stage of the customer identity lifecycle this milestone's brief named explicitly: purchase, customer, customer account, customer profile, purchase session, order, payment, review eligibility. Every value below was read directly from the production database, not inferred from code.

## Purchase

`purchase_sessions` id 46:
- `purchase_reference`: RWL-2026-000046
- `status`: verified
- `provider`: paystack, `provider_status`: success
- `amount_pesewas`: 900 (GH₵9.00)
- `created_at`: 2026-07-31 18:25:27, `verified_at`: 2026-07-31 18:26:31

Exists and is correctly verified. No defect found at this stage.

## Payment

`payment_transactions` id 21:
- `paystack_reference`: RWL-2026-000046, `event_type`: charge.success, `status`: success
- `gateway_response` contains a real, live Paystack transaction (`"domain":"live"`, `channel: "mobile_money"`, bank MTN, real transaction id 6411673100)
- `webhook_received_at` and `verified_at` both populated at 2026-07-31 18:26:31

Confirmed genuine: this was a real live payment, not a sandbox or synthetic test transaction. No defect found at this stage.

## Customer

`customers` id 9:
- `email`: checkout+rwl-2026-000046@robayerwealthlab.com (synthetic placeholder -- see the Authentication and Email Delivery Root Cause Reports for why)
- `password_hash`: null (no password ever set)
- `status`: active
- `created_at`: 2026-07-31 18:26:31 (same instant as purchase verification, confirming auto-provisioning fired correctly)

Row exists and was created correctly by the provisioning flow. The only defect is the email value it was provisioned with, not the provisioning mechanism itself.

## Customer profile

`customer_profiles` (customer_id 9):
- Row exists, created at 2026-07-31 18:26:31, all fields at their correct defaults for a brand-new profile (no display name, no marketing opt-in, no avatar).

Exists and correctly seeded. No defect found.

## Order artifacts

- `order_items` id 5: linked to purchase_session_id 46, correct product/title/price/quantity.
- `licenses` id 5: linked to purchase_session_id 46 and customer_id 9, a real license key issued at verification time.
- `receipts` id 5: linked to purchase_session_id 46 and customer_id 9, correct line items/subtotal/total, a PDF storage key present.
- `deliveries` id 20: linked to purchase_session_id 46, status `delivered`, `downloads_used: 1` (the customer did successfully download the ebook via the callback page, consistent with the brief's own statement that "the ebook download authorization works").

All four order artifacts exist, are correctly linked, and are internally consistent. No defect found in order/fulfilment logic.

## Review eligibility

`product_reviews` before this milestone's repair: no row for customer 9. Not because eligibility logic rejected the customer, but because the customer could never authenticate far enough to reach the eligibility check at all (`requireCustomerAuth()` runs before any ownership/eligibility logic in `handleSubmitReview()`).

## Root cause of every gap in this lifecycle

Every stage above that depends on payment and fulfilment logic (purchase, payment, order artifacts) is correct and required no repair. Every stage that depends on customer identity (customer, customer profile, and everything downstream: password, sign-in, review) was blocked by exactly one upstream defect: the checkout flow never captured this customer's real email. Full root-cause detail is in the Authentication Root Cause Report and the Email Delivery Root Cause Report.

## Post-repair state (verified live)

After the checkout fix was deployed and the existing real customer record was corrected to a real, monitored email (a data repair on a genuinely existing customer, not a new fabricated one):

- `customers.password_hash` is now set (password successfully created via the real set-password endpoint using a real, database-retrieved token).
- A real customer session was issued by the real sign-in endpoint.
- `product_reviews` now has a row for customer 9 against the product they actually purchased (`reviewId: 2`, status `pending`, awaiting the normal moderation step).
- `purchase_sessions.customer_id` for id 46 was confirmed to correctly re-link via the reconciliation ("recover purchase") flow after a reversible orphaning test, proving that flow works for any future real customer who ends up in a similar orphaned state.

The lifecycle is now complete end to end for this customer, verified through real execution against production, not code inspection.
