This folder is retired. The placeholder `logo.svg` that used to live here
(a coded approximation of the Sika step-mark, added during the Sprint 1.5
cleanup so the JSON-LD `logo` field had something to resolve to) was
removed during the Sprint 18 production-readiness audit once it became
fully unreferenced — the header/footer nav mark and every page's
JSON-LD `logo` field now point at the real production logo in
`assets/branding/logo/` (see that folder's README).

If a future page or script ever needs a generic "images" folder again,
this one is still here to reuse — just don't resurrect the old
placeholder file.
