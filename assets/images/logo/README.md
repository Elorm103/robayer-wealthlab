Final vector logo artwork (Option D primary symbol, Option E combination
lockup, monogram, and all light/dark/monochrome variants approved in
Phase 2.5) goes here as production SVG/PNG files once produced.

The inline SVG currently used in partials/header.html is a coded
approximation of the approved Sika step-mark concept, sufficient for the
foundation phase — it should be replaced with the final production
artwork file(s) before launch.

Status: logo.svg in this folder is the same coded placeholder mark,
added during the Sprint 1.5 cleanup so the JSON-LD `logo` reference
resolves. Replace it (and og-default.jpg in assets/images/) with final
production artwork before launch.
